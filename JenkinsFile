pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello, World!'  // Prints "Hello, World!" in the Jenkins console output
            }
        }
    }
}